<div class="container">
  <h1>Result</h1>
  <% if @well_done%>
    <p><strong>Congratulations!</strong> <%= @word %> is a valid english word!</p>
  <% elsif @not_an_english_word%>
    <p>Sorry but <strong><%= @word %></strong> does not seem to be an english word...</p>
  <% else %>
    <p>Sorry but <strong><%= @word %></strong> can't be buit out of <%= @letters %></p>
  <% end %>
  <p>Your score:</p>
  <p><%= session[:score] %></p>
  <%= link_to "Play again", new_path%>
</div>
